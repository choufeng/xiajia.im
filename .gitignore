node_modules/
docs/.vitepress/cache/
docs/.vitepress/dist/
.DS_Store

# Convex 生成代码（npx convex dev / deploy 时自动再生）
convex/_generated/
# 本地开发时的 Convex deployment 地址（含 dev 部署 URL）
.env.local
.env.*.local

# 音频存 COS，不入仓库
docs/public/audio/
docs/public/tts/

# pi 运行时产物（子代理会话、临时 skill）
.pi-subagents/
.pi/skills/

# 临时工作文件
progress.md
research.md
__pycache__/
*.pyc

# 偏私有日历订阅：ICS + JSON 进 git（CI 部署需要）。token 是 unlisted 防线
