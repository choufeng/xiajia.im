# 项目铁律（本项目级，覆盖全局）

## Git 工作流

- **本项目在 `master` 分支直接开发、直接提交、直接推送。**
- **不受**全局 AGENTS.md「禁止 main 分支直接提交」「必须新建分支」规则的约束。
- **无需**为任何改动新建功能分支、修复分支或 PR 分支。
- 任何代码、文档、配置变更，在 `master` 上完成后立即 commit & push。

> 本规则为项目最高优先级约束。与全局规则冲突时，以本文件为准。
